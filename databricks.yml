# This is a Databricks asset bundle definition for lab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: lab
  uuid: 5b8581ed-842c-4f93-b7c4-4c287017ba56

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-3549239056501620.0.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-3549239056501620.0.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/admin@mngenvmcap292602.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/admin@mngenvmcap292602.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: admin@mngenvmcap292602.onmicrosoft.com
        level: CAN_MANAGE
